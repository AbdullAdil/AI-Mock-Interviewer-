{
  "name": "ai-mock-interviewer",
  "version": "0.1.0",
  "private": true,
  "description": "Root workspace for the AI Mock Interviewer. Use `npm run dev` to boot both client and server.",
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"blue,green\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

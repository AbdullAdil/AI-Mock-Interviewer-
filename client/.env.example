# In production, set this to your Railway backend URL with no trailing slash,
# e.g. https://ai-mock-interviewer-server.up.railway.app
# In development, leave this empty — Vite's dev proxy forwards /api/* to localhost:3001.
VITE_API_URL=
